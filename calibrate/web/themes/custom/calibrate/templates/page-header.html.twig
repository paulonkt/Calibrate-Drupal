{# page-header.html.twig #}
{% if node is defined and (node.bundle == 'articles' or node.bundle == 'news') %}
  <div class="page-header">
    <div class="overlay">
      <h1 class="page-title">{{ node.label }}</h1>
    </div>
  </div>
{% endif %}
